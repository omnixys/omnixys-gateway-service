name: Deploy Omnixys Service

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          cat << 'EOF' > ~/.ssh/id_ed25519 ${{ secrets.SERVER_SSH_KEY }} EOF
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H api.omnixys.com >> ~/.ssh/known_hosts

      - name: Deploy service
        run: |
          ssh root@api.omnixys.com << EOF
            set -e

            SERVICE="${{ secrets.SERVICE }}"

            echo "📦 Deploying service: \$SERVICE"
            cd /opt/omnixys/compose

            echo "🔄 Pulling image"
            docker compose pull \$SERVICE

            echo "🚀 Restarting container"
            docker compose up -d \$SERVICE

            echo "🧹 Cleaning old images"
            docker image prune -f --filter "until=168h"

            echo "✅ Deploy of \$SERVICE finished"
          EOF
